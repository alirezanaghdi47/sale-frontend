// libraries
import React from "react";

export type PrivateLayoutType = {
    children: React.ReactNode
}

export type ProtectedLayoutType = {
    children: React.ReactNode
}

export type PublicLayoutType = {
    children: React.ReactNode
}

export type RootLayoutType = {
    children: React.ReactNode
}